# Yuashie 0.3.2 · 2026.08.31

基于完整 0.3.1 源码；页面功能为前端更新，另按站主要求配置了 GitHub 同步后的自动前端发布。

## 变化

- `/about` 改为更新日志，移除原会员介绍，保留已有独立 `/vip` 页面。
- 记录可核对的 0.1.6、0.2.1、0.2.4、0.2.5、0.2.6、0.3.0、0.3.1 和本次 0.3.2；早期建站过程单独列出，不补造版本号。
- 日期按北京时间。0.2.1 于 08.29 完成初版、08.30 补充徽章；0.2.4 和 0.3.0 的交付跨过午夜，分别记录为 08.30、08.31。
- 新增 `/terms` 用户服务条款、`/privacy` 用户隐私协议、`/works` 作品使用条款。
- About、全站页脚和注册页提供协议入口。首页改用既有公共页脚，保留版权及备案信息。
- 电台资料、通联日志及 QSL 页面增加公开范围提示，避免误把当前公开功能理解为私密存储。
- 新增内容支持中文、英文和日文，延续日夜主题及手机布局。

## 重要边界

这些条款是按当前源码和已知网站情况起草的站点文本，不是律师审核意见或完整合规认证。没有新增“浏览即同意”、默认勾选或伪造用户同意记录。本次未增加协议确认数据库、自助注销、批量导出、未成年人监护人验证、呼号验证或私密日志开关，也未改变现有 API 数据公开方式。

当前源码实际行为：

- 个人资料、徽章与近期贡献记录公开，邮箱不在公开资料 API 中。
- 电台主页公开最近 20 条通联摘要，包含双方呼号、时间、频率、模式、信号报告、功率、QTH、设备、天线和 QSL 状态，不含备注。
- 发 QSL 会保存完整通联快照（含备注）；修改/删除原日志不会自动撤回快照。
- QSL 图片地址不要求登录；上传不自动去除 EXIF。删除已被发送卡片引用的模板，不会立即删除底图文件。
- 默认登录 Cookie 有效期 30 天；退出会撤销当前会话。Cookie 到期不表示数据库旧会话记录已经自动物理删除。

## 正式发布前的运营核对

1. 核对站主对外称谓、联系方式与工单处理能力。现使用已经存在的站内反馈中心；无法登录的用户通过仓库 Issues 请求私下联系，并明确禁止公开提交敏感材料。建议后续补充经站主确认的专用隐私联系邮箱，未擅自使用私人邮箱。
2. 核实实际云服务商、服务器地域、日志/备份保留和清理周期及跨境情况。本次没有凭空填写供应商合同、固定保留天数或“绝不出境”等保证；服务器实际配置须与政策一致。
3. 若面向公众扩大注册，完善条款确认及可追溯记录、必要的单独同意机制、注销与删除流程；当前仅增加阅读入口，不声称已取得有效同意。
4. 针对公开电台资料、他人呼号与通联信息及图片元数据，审核适用的信息处理依据；必要时另行实施可见性控制与明确同意。本次没有通过文字宣称现有处理必然合法。
5. 软件以独立 LICENSE 为准；作品默认保留权利，本次不为现有项目擅自添加开源许可证。

法律参考（核对日期：2026.08.31）：

- 隐私告知、权利及删除规则参考[《个人信息保护法》](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm)，包括第17、28—31、44—50条。
- 格式条款提示及责任边界参考[《民法典》](https://www.moj.gov.cn/pub/sfbgw/zwgkztzl/2025nianzhuanti/2025mfdxcy/2025mfdxcy_mfdql/202505/t20250507_518708.html)，包括第496—497条。
- 作品权利与合理使用参考[2020年著作权法修改决定](https://www.nppa.gov.cn/xxfb/ywxx/202011/t20201112_664217.html)。

## 更新方法

将压缩包解压覆盖到现有项目根目录（有 package.json 的目录），不要删除原目录。重新启动预览，确认后使用原有发布方式更新 yuashie.cn。

不包含真实 `.env`、数据库、用户上传内容、node_modules 或 dist；不要删除服务器已有配置和数据。后端与 SQL 迁移和 0.3.1 保持一致，本次无需新增迁移。GitHub 仓库保留并增强了已有 `.github` 发布流程：main 的前端更新默认自动发布；同时比较上次公开部署，遇到待升级的后端、数据库或服务器接收程序时先停止。既有部署密钥和严格 SSH 主机校验保持不变。先前下载的源码包是该流程增强前的快照，后续请以 GitHub 最新源码为准。

如果查看 ChatGPT 内提供的页面版本，它仅用于查看静态前端。账号、评论、Minecraft、无线电保存和上传仍依赖你原来的 API 与数据库，不能以静态页面展示替代服务器上线验证。

## 验证

- 校验 8 个版本条目、9 份三语协议内容及对应章节、隐私表格完整性。
- Vue 组件编译检查和生产构建。
- 与 0.3.1 基线逐文件比较，确认后端、SQL、配置示例、Minecraft 插件和发布脚本未改变。
- 未连接生产账户或数据库，未执行真实注册、发卡或隐私请求。
