# Yuashie 0.6.0 · 个人节点的模块化重绘

参考站：https://winmoes.com/ （2026-09-25 实际浏览）

## 参考分析与设计转译

| 维度 | 参考站观察 | Yuashie 的实现 |
| --- | --- | --- |
| 信息架构 | 顶部工具条、分类导航、大小组合的精选区，随后是分类内容和辅助侧栏 | 最新动态、主卡与个人信息侧栏、精选项目、实验室与更新、宣言；沿用原路由 |
| 排版 | 图片吸引注意，标题次之，分类、作者与日期降低权重 | 收敛大标题字号；中文正文保持无衬线；编号、版本等工业标记用等宽字体 |
| 卡片 | 统一圆角和边界，封面区与内容元信息有稳定层次 | 项目改为上图下文，独立显示状态；12px 圆角、细边框与轻阴影 |
| 导航 | 分类导航与搜索、登录等工具入口分工明确 | 保留所有功能入口，增加首页和明确选中态；小屏采用可折叠网格菜单 |
| 间距 | 密集内容通过规则网格与重复间隔组织 | 20px 卡片间隔、22–40px 内边距、30–40px 区块间隔；桌面最大内容宽度 1280px |
| 动效 | 观察到链接采用约 0.2s 的透明度过渡；轮播与浮动工具承担辅助作用 | 卡片轻微上移、封面亮度变化，保留已有交互；不新增自动轮播；减少动态效果时取消位移 |
| 明暗关系 | 实际页面有深色背景图、明亮导航及面板、图片上的文字遮罩，并非纯白极简页面 | 抽取层次与模块化组织：默认石墨深色，浅色主题用灰白底与白卡；主视觉维持深色以建立焦点 |
| 视觉层级 | 精选内容优先，分类内容次之，元信息与侧栏降低对比 | 主卡 > 项目标题 > 摘要 > 状态与编号；荧光绿只标记重点和可操作元素 |

没有引入参考站的商标、插画、图片、字体文件或代码。主页几何轨道与网格由 CSS 构成；项目图片仍使用 Yuashie 既有素材。

## 代码与行为

- `src/modular-ui.css`：集中定义颜色、模块布局、导航、卡片、表单面板与响应式覆盖，便于继续维护与独立回退。
- `src/views/HomeView.vue`：重组首页为一个完整 main；保留项目、探索、搜索、游乐室、实验室和更新记录入口；项目状态沿用现有数据。
- `src/components/SiteNav.vue`：增加首页；修复原基础样式在小屏隐藏链接的问题；日文等长导航在 1240px 以下使用折叠菜单。
- `src/main.js`：加载模块化视觉层。路由、API、身份认证和后端文件没有变更。
- 更新版本与三语记录，0.5.0 历史记录独立保留。

## 验证

- `npm run build`：生产构建通过。
- `npm test`：11 项现有测试全部通过（搜索、摩尔斯电码、偏好、API 请求、2048 与翻译）。
- 浏览器检查：桌面浅色/深色、390px 手机首页及项目页、展开菜单和搜索结果；中英日切换。
- 320px 宽度：主页、探索、实验室、游乐室、电台、登录、反馈、项目详情、关于页均加载主要内容，无文档水平溢出。
- 日文中等宽度导航溢出在检查中被发现，并通过提前切换为折叠菜单修复。
- 登录后的账号、QSO 写入、QSL 上传及管理操作未做真实数据写入测试；本次未改变这些逻辑。

生产发布状态以 GitHub Actions 与线上 deploy-version.json 核验为准，本文件不把本地构建等同于上线。
