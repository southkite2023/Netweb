# Yuashie 0.3.1 更新说明

基于包含无线电台功能的 0.3.0 完整源码，仅调整前端体验。

## 本次修改

1. 评论或回复在服务器确认提交成功后，弹出“评论成功”窗口；点击“确定”或按 Escape 关闭。提交失败仍显示错误，不弹出成功窗口。
2. 每次路由跳转完成后立即回到页面顶部，包括从主页进入项目、项目之间跳转和历史前进/后退。
3. 全站右下角增加 TOP 按钮，页面离开顶部时出现，回到顶部后隐藏。支持移动端安全区，点击平滑回顶；系统开启减少动态效果时直接回顶。
4. 首次使用新版默认中文，不再继承旧版自动写入的 English 设置。用户手动选择中文、英文或日文后，新版会记住该选择。旧版本的语言偏好也会在本次升级后重置为中文一次。

提示文字及按钮的无障碍标签均提供中文、英文、日文。

## 更新方法

将压缩包内容解压覆盖到 Windows 上现有项目根目录（包含 package.json 的目录），不要删除原目录。重启本地预览进程，再使用已有的 Yuashie Preview / Yuashie Publish 快捷方式预览和发布。

本次未修改服务器代码、数据库迁移或发布脚本。包内不包含真实 .env、数据库、用户上传文件、node_modules 或 dist；保留原有配置和数据，不要删除它们。仍需通过原有发布流程才能让线上网站生效。

若服务器已经运行 0.3.0，本次只需要更新前端构建。若尚未运行 0.3.0，应按 RADIO-SYSTEM-0.3.0.md 和原有完整发布流程完成无线电台功能的升级。

## 验证情况

已通过依赖锁定安装及 npm run build 生产构建；已检查此次差异中服务器目录与 0.3.0 一致。未连接生产账号发表评论，未执行线上发布或 GitHub 同步。
